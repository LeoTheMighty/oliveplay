apiVersion: v1
kind: Service
metadata:
  name: {{ include "cate-db.fullname" . }}
  labels:
    {{- include "cate-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cate-db.selectorLabels" . | nindent 4 }}
