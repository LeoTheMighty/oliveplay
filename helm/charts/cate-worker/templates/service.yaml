apiVersion: v1
kind: Service
metadata:
  name: {{ include "cate-worker.fullname" . }}
  labels:
    {{- include "cate-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cate-worker.selectorLabels" . | nindent 4 }}
